# 未來計畫待辦事項

> 最後更新：2026-03-22 | 本文件為專案未來改版的先驅研究與規劃追蹤

---

## 待納入（需進一步釐清後歸入正式分類）

### N1. 以 Skill + CLI 強化系統功能

- **構想**：讓 `cursor-cli` Skill 與 Claude Code CLI 協作，擴展系統可用的執行引擎
- **關聯**：`cursor-cli` Skill（#37）、`claude -p` CLI
- **下一步**：
  1. 定義 CLI 任務分流規則（哪些任務用 Claude、哪些用 Cursor Agent）
  2. 評估 `agent -p` 的 prompt 格式與 `claude -p` 的相容性
  3. 建立統一的結果 JSON Schema，確保兩種引擎輸出可互通

### N2. LINE 可接受系統優化 / 檢測 / 特定待辦的指令

- **構想**：透過 LINE Webhook 讓使用者下達「執行系統審查」「查詢待辦」等指令
- **關聯**：5.2 LINE Webhook + Gun Relay 整合、`chatroom-query` Skill、KB 筆記 `418e0c9c`（LINE Bot Webhook Agentic 任務管線）
- **KB 補充**：KB 研究指出 Azure AI Agent Orchestration 的 Sequential Pipeline + Maker-Checker Loop 設計可作為指令處理管線藍本
- **下一步**：
  1. 定義 LINE 指令 → Agent 任務的映射表（如 `/audit` → `run-system-audit-team.ps1`）
  2. 評估 Gun.js relay 作為指令中介層的可行性（已有 `bot/bot.js` 基礎）
  3. 安全考量：指令白名單 + 使用者身份驗證
  4. 參考 Azure AI Agent Orchestration Patterns 設計 Maker-Checker 確認機制

### N3. 優化 Todoist Skill

- **構想**：提升 Todoist Skill 的查詢效率、過濾精確度與更新可靠性
- **關聯**：`todoist` Skill（#1）、`config/scoring.yaml`、KB 筆記（Todoist API v1 整合實務）
- **下一步**：
  1. 評估 Todoist 官方 MCP Server（`todoist-ai`）取代手工 curl 的可行性（KB 建議）
  2. 強化 `filter` 端點使用（`/api/v1/tasks/filter?query=`），減少全量拉取
  3. 為 recurring 任務的更新加入 `is_recurring` 保護驗證

### N4. 系統自動從 todo.txt 讀取待辦由指定模型執行

- **構想**：建立 `todo.txt` → Agent 任務派發管線，使用者只需編輯純文字清單
- **關聯**：`task-manager` Skill（#20）、`todoist-task-creator` Skill（#23）
- **下一步**：
  1. 定義 `todo.txt` 格式規範（支援優先級、模型指定、標籤）
  2. 建立 watcher 腳本（PowerShell FileSystemWatcher 或排程掃描）
  3. 與現有 Todoist 管線整合（todo.txt 條目同步至 Todoist 或直接派發）

### N5. LLM-as-Judge Agent 輸出品質自動評估

- **構想**：引入 LLM-as-Judge 模式，自動評估 Agent 輸出品質，取代純人工抽查
- **關聯**：KB 筆記（AI Agent 評估框架與基準測試方法論）、`quality-tracker` Skill（#39）、`config/audit-scoring.yaml`
- **KB 研究基礎**：KB 已有 GAIA/SWE-bench 評估框架研究，建議四維度量（目標達成 + 品質 + 效率 + 安全）
- **下一步**：
  1. 定義 Agent 輸出 Rubric（以 `audit-scoring.yaml` 7 維度為基礎，擴充 Agent 級評分）
  2. 建立語義一致性基線：追蹤 tokens/successful-task 效率指標
  3. 先用 Groq 輕量模型做 Phase 3 assemble 時的品質打分（成本可控）

### N6. Session-Scoped 快取生命週期管理

- **構想**：引入「Session 範圍快取」概念，Agent Team 執行完畢後批次清理快取，避免跨 session 汙染
- **關聯**：KB 筆記（Zed GPUI Thread-Local Arena 模式）、`api-cache` Skill、`cache/status.json`
- **KB 研究基礎**：Zed 編輯器的 `ELEMENT_ARENA` 模式——每 frame 分配、frame 結束整體清除——可啟發「session-scoped cache」設計
- **下一步**：
  1. 定義 session 邊界（一次 `run-agent-team.ps1` 或 `run-todoist-agent-team.ps1` 執行）
  2. Phase 0 標記 session_id 到 `cache/status.json`，Phase 3 完成後清理該 session 的暫存快取
  3. 評估與現有 TTL 機制的共存策略（TTL 管長期、session 管短期）

### N9. 5-tier 分級自癒機制 🆕

- **構想**：引入分級自動恢復，取代現有「單次重試 + ntfy 告警」的簡陋模式
- **關聯**：GitHub Scout `openclaw-self-healing`（31 stars，9/14 事件完全自動解決）、KB 筆記 `854fb55b`（可觀測性缺口與故障自愈升級方案）
- **來源**：improvement-backlog 2026-03-22 GitHub Scout 自癒主題
- **KB 研究補充**：
  - KB 指出現有 self-heal 只做清理不驗證根因修復，需補齊「修復後驗證」步驟
  - 若 `system-insight.json` 自身也零執行，自愈迴圈退化為「只做清理」
  - Runtime override（帶 `reason`、`expires_at`、blocked targets）優於硬重啟
- **設計分級**：
  - **Tier 0**：Preflight 配置驗證（Phase 0 前檢查）
  - **Tier 1**：即時重試（< 30s，現有能力）
  - **Tier 2**：Watchdog 健康檢查（3 分鐘間隔，確認恢復）
  - **Tier 3**：AI 診斷（30 分鐘失敗後，LLM 分析 logs 提出修復建議）
  - **Tier 4**：人工告警（ntfy 通知 + 待批准行動）
- **下一步**：
  1. 在 `config/recovery-tiers.yaml` 定義五層恢復策略（觸發條件、動作、上升條件）
  2. 擴充 `tools/autonomous_recovery_worker.py`，加入 Tier 2 健康驗證與 Tier 3 AI 診斷
  3. 建立 crash counter 與 6 小時自動衰減（參考 openclaw-self-healing），避免偶發性失敗長期標記
  4. 修復後驗證閉環：清理後等待下次排程確認恢復正常才結案

### N10. LLM-Agnostic 統一環境變數設計 🆕

- **構想**：統一 LLM 提供者切換為單一 `LLM_PROVIDER` 環境變數 + routing_rules 映射
- **關聯**：GitHub Scout `openclaw-self-healing`（LLM-Agnostic 設計）、`config/llm-router.yaml`、`tools/llm_router.py`
- **現況**：已有 `llm-router.yaml` 與 `groq-relay`，但配置分散於多處
- **下一步**：
  1. 定義 `LLM_PROVIDER` 環境變數，支援 Claude（預設）、Groq（快速摘要）、Ollama（離線備援）
  2. 統一 routing_rules 映射：一個入口點自動選擇提供者
  3. 離線備援模式：Groq 失敗回退 Claude、Claude 失敗回退 Ollama

### N11. Prometheus 可觀測性指標匯出 🆕

- **構想**：暴露系統關鍵指標為 Prometheus metrics，建立時序趨勢分析能力
- **關聯**：GitHub Scout `openclaw-self-healing`（Prometheus exporter）、`check-health.ps1`、5.5 統一 Trace Schema
- **現況痛點**：`check-health.ps1` 僅輸出文字報告，無時序指標與長期趨勢
- **下一步**：
  1. 建立 `tools/prometheus_exporter.py`：暴露 daily_success_rate、cache_hit_ratio、auto_task_fairness、恢復成功率
  2. 評估 Grafana Cloud Free Tier 作為視覺化方案
  3. 與 5.5 統一 Trace Schema 整合，共用指標來源

---

## 一、系統穩定性（P0 — 立即影響執行品質）

> 本區項目直接影響日常自動化的成功率與資源效率，應優先處理。

### 1.1 自動任務公平輪轉修復 ✅ → 持續觀察

- **狀態**：ADR-20260318-027 **已完成**（fairness-hint 預計算已上線）
- **成果**：公平性指標從 1.403 改善至 1.211（-13.7%），但仍超標（門檻 0.5）
- **殘餘追蹤**：
  - 29 個任務中今日僅 13 個有執行記錄，覆蓋率需持續提升至 ≥60%
  - daily_limit=0 的暫停任務（unsloth/creative_game/qa/chatroom）造成假性不公平，需明確廢棄或恢復
- **驗收目標**：7 天後 Gini 係數 < 1.0、executed task 覆蓋率 ≥60%

### 1.2 Phase 2 快照恢復與故障續跑 ✅

- **狀態**：ADR-20260318-028 **已完成**（Phase 2 snapshot-resume 邏輯已上線）
- **成果**：Phase 2 部分失敗可從 snapshot 續跑，不重複已成功 Agent
- **殘餘追蹤**：持續監控 snapshot 使用頻率與 resume 成功率

### 1.3 高失敗時段根因分析與時段風險評估 🟡

- **目標**：識別高失敗時段（5、7、13 時），建立時段風險評分模型與預防性檢查機制
- **優先級**：P1（短期，ADR-20260322-037 Proposed/pending）
- **關聯**：`pre-flight-check` Skill（#45）、`system-insight` Skill、Google SRE Golden Signals
- **最新數據**（2026-03-22）：daily_success_rate=0.865（持平，門檻 0.95）；健康檢查偵測到逾時+Phase 失敗
- **風險評分公式**：`risk_score = 歷史失敗率×0.4 + 外部維護×0.3 + 並行負載×0.2 + 系統資源×0.1`
- **KB 補充**：KB 指出 self-heal 只做清理不驗證根因，建議補齊「修復後驗證」步驟
- **可執行動作**：
  1. 掃描近 30 天 logs，按小時統計失敗率與失敗類型，產出 `analysis/hourly-failure-pattern.json`
  2. 盤點外部服務維護時間窗口，產出 `config/external-sla.yaml`
  3. 建立 `tools/time_slot_risk_scorer.py`，計算各時段 risk_score
  4. Phase 0 加入風險閘門：`risk_score > 0.7` 降級、`> 0.9` 跳過非關鍵任務並 ntfy 告警
  5. 觀察 1 週記錄降級策略生效次數與成功率變化

---

## 二、Agent 能力強化

### 2.1 Skill 直接優化 — 更嚴格的指令控管

- **目標**：提升 Skill 執行品質，減少 Agent 自由發揮導致的格式漂移與不一致
- **優先級**：P1（短期）
- **關聯**：ADR-001 Skill-First 策略、`agent-result-validator` Skill（ADR-003）、`prompt-lint` Skill
- **現況**：55 個 Skills 已建立（20 核心 + 35 工具），但部分 SKILL.md 指令不夠嚴格
- **可執行動作**：
  1. 用 `prompt-lint` Skill 對所有 SKILL.md 做靜態分析，找出指令模糊點
  2. 參考 `agent-result-validator` 的 Schema 驗證機制，為高頻 Skill（todoist、knowledge-query、hackernews-ai-digest）加入輸出 Schema 強制
  3. 建立 Skill 品質基線：每個 SKILL.md 必須包含「禁止行為」和「輸出範例」兩個段落

### 2.2 Skill 分層管理（三層 Skill 系統）

- **目標**：引入 core → shared → workspace 三層 Skill 分層機制，提升生命週期管理
- **優先級**：P1（短期～中期）
- **關聯**：improvement-backlog 提案（源自 OpenClaw 302K Stars）、`skills/SKILL_INDEX.md`、n-skills openskills 格式
- **已完成基礎**：ADR-20260320-033（Machine-readable Skill Registry）已建立 `context/skill-registry.json`（52 Skills）
- **建議分層**：
  - **core**（~20 個）：每日摘要必用、高穩定度（todoist、pingtung-news、ntfy-notify、api-cache 等）
  - **shared**（~10 個）：跨專案共用、中等穩定度（knowledge-query、web-research、groq 等）
  - **workspace**（~23 個）：實驗性或低頻使用（skill-forge、workflow-forge、behavior-pattern-analyzer 等）
- **可執行動作**：
  1. 在 `context/skill-registry.json` 加入 `tier` 欄位（core|shared|workspace）
  2. 制定分層升降規則：workspace Skill 累積 30+ 次成功執行後可升級為 shared
  3. 用 `skill-lifecycle-manager` Skill（#50）產出穩定度評分與分層建議

### 2.3 持久記憶功能 / Agent 及排程長記憶方案

- **目標**：實現跨 Session 的長期記憶，讓 Agent 能累積知識與追蹤長期趨勢
- **優先級**：P1（短期～中期）
- **關聯**：`digest-memory` Skill、`context/continuity/` 連續記憶機制、KB 筆記 `b1640648`、`1adc3ffd`（Hindsight）、`14c8dbd1`（Mastra）
- **現況**：
  - 已有 `digest-memory.json`（跨次摘要記憶）和 `context/continuity/auto-task-*.json`（自動任務連續記憶）
  - 缺乏**語義記憶整合**（KB 知識與 Agent 決策的橋樑）和**記憶衰減機制**
  - `tools/autonomous_harness.py` 已實作自治 supervisor，可作為記憶整合層底座
- **KB 研究基礎（2026-03 最新）**：
  - Mastra 雙 Agent 壓縮：Observer（未觀察訊息超 30K tokens 時壓縮）+ Reflector（觀察日誌超 40K tokens 時合併移除過時資訊）——可借鑑為 continuity 壓縮策略
  - **Hindsight**（MCP 原生整合，LongMemEval 91.4% SOTA）：ROI 最高
  - Mem0 效能數據：26% 準確率提升、91% 延遲降低、90% Token 節省
- **ROI 評估框架**：

  | 方案 | 整合難度 | 本專案適配度 | 維護成本 |
  |------|---------|-------------|---------|
  | 現有 JSON 方案 | 已完成 | 高（自建） | 低 |
  | Mem0/Mem0g | 中等 | 高（跨 Session） | 中 |
  | Hindsight + MCP | 中等 | **最高**（MCP 原生） | 低 |
  | Mastra Observer | 高 | 中（TypeScript） | 中 |

- **可執行動作**：
  1. 設計「記憶整合層」：自動將 `continuity/*.json` 的重複模式提煉到 KB 筆記
  2. 為 `digest-memory.json` 加入趨勢摘要欄位（7 日/30 日滑動視窗）
  3. **優先評估 Hindsight + MCP**：因 MCP 原生整合與本專案架構最契合
  4. 借鑑 Mastra 雙 Agent 壓縮模型（Observer 30K + Reflector 40K）優化 continuity 檔案裁剪策略

### 2.4 排程可以由系統安排

- **目標**：讓 Agent 根據任務負載與歷史模式，自動建議或調整排程
- **優先級**：P2（中期）
- **關聯**：`scheduler-state` Skill、`task-fairness-analyzer` Skill、`HEARTBEAT.md`、KB 筆記 `3198e38d`
- **現況**：排程完全由 `HEARTBEAT.md` 靜態定義 + `setup-scheduler.ps1` 寫入 Task Scheduler
- **可執行動作**：
  1. 用 `system-insight` Skill 分析歷史執行時段分布，找出低效率時段
  2. 建立「排程建議引擎」：基於 Gini 係數 + KB 排程研究，產出排程調整建議
  3. 可與 1.3 時段風險評估整合，risk_score 高的時段自動降低排程密度

### 2.5 Agent 間通訊機制

- **目標**：在 Team 模式 Phase 2 並行 Agent 間建立輕量通訊管道
- **優先級**：P2（中期）
- **關聯**：improvement-backlog 提案（源自 OpenClaw sessions_list/history/send 模式）
- **設計概念**：Phase 2 結果 JSON 新增 `inter_agent_messages` 欄位，Phase 3 assemble 時讀取並轉發
- **可執行動作**：
  1. 定義 `inter_agent_messages` 的 JSON Schema（sender、receiver、type、payload）
  2. 先在 2 個高頻任務間試行（如 `ai_deep_research` → `future_plan_optimize`）

---

## 三、系統品質提升

### 3.1 跨平台路徑硬編碼清理 ✅

- **狀態**：ADR-20260318-025 **已完成**
- **殘餘項**：持續監控新增檔案是否引入硬編碼路徑

### 3.2 輸入驗證強化（Schema 驗證）✅

- **狀態**：ADR-20260318-026 **已完成**
- **殘餘項**：為更多 `results/*.json` 類型建立 Schema 定義

### 3.3 Context Budget Guard 強制化 ✅ → 持續強化

- **狀態**：ADR-20260317-022 **已完成**
- **殘餘追蹤**：avg_io_per_call 仍達 26,567（超標 5.3 倍），需更積極壓縮策略（見 3.9）

### 3.4 快取命中率調優與自適應 TTL ✅ → 持續觀察

- **狀態**：ADR-20260319-029 **已完成**
- **最新數據**：cache_hit_ratio=0.2167（微升 +0.4pp），仍低於 0.4 門檻
- **驗收追蹤**：2026-03-26 前驗證 cache_hit_ratio ≥40%

### 3.5 TODO/FIXME 分批清理

- **目標**：將 369 處 TODO/FIXME（76 個檔案）分類為「規範性」vs「缺陷型」，分批清理
- **優先級**：P1（短期，持續進行）
- **關聯**：ADR-017、`todo-scanner` Skill
- **可執行動作**：
  1. 完善 `todo-scanner` Skill 從草稿升級為正式版
  2. 第一批清理：`docs/plans/`、`templates/`、`prompts/` 下的規範性 TODO
  3. 建立 CI 閘門：新增 TODO 必須附帶 issue 編號或 ADR 引用

### 3.6 測試覆蓋率提升

- **目標**：維持 44% 以上並逐步提升至 60%（目前 1086 個測試基線）
- **優先級**：P2（中期，ADR-20260311-011 ongoing）
- **可執行動作**：
  1. 優先補強 `hooks/hook_utils.py`（復用率 63.6%→80%+）
  2. 為 `tools/llm_router.py`、`tools/budget_guard.py` 補充邊界條件測試
  3. 設定 pytest-cov 最低門檻（`--cov-fail-under=44` 起步，逐季 +5%）

### 3.7 Skills 測試覆蓋擴充

- **目標**：從 3/55 Skills 有測試提升至 10+
- **優先級**：P2（中期）
- **關聯**：`skill-test-scaffolder` Skill
- **可執行動作**：
  1. 用 `skill-test-scaffolder` Skill 自動生成測試骨架
  2. 優先為 `todoist`、`knowledge-query`、`api-cache` 三個高頻 Skill 補測試

### 3.8 YAML 注釋密度提升

- **目標**：從 10.6%-13.2% 提升至 20% 目標
- **優先級**：P2（中期，低成本）
- **KB 補充**：KB 指出 YAML 配置已達 4,840 行（+5%），認知負擔持續增加，注釋可降低閱讀成本
- **可執行動作**：
  1. 為每個 YAML 配置檔的關鍵段落補充 `#` 注釋
  2. 建立注釋密度自動檢查（整合進 `check-health.ps1`）

### 3.9 實時 Token 預算監控與 Context Compression 🟡

- **目標**：建立 per-phase token budget 監控、超限自動壓縮、成本歸因追蹤
- **優先級**：P1（短期，ADR-20260322-035 + ADR-20260322-036 均為 Proposed/pending）
- **關聯**：`tools/budget_guard.py`、`config/budget.yaml`、KB 筆記 `ec882cc4`（Context Window 治理）、`79d30f1a`（Token 經濟學）
- **KB 研究基礎（2026-03-22 更新）**：
  - **0.75 × max_tokens 閾值**觸發壓縮（LangChain Deep Agents SDK / 業界共識）
  - **Token 經濟學**：80% 方差來自 Token 預算分配，非架構選擇——優先治理預算 ROI 最高
  - **per-trace 成本歸因**已是生產標準（Sentry、LangSmith、Datadog、Langfuse 均支援）
  - Mastra 雙 Agent 壓縮：Observer 30K + Reflector 40K tokens 閾值自動壓縮
  - 建議 `config/token-budget.yaml`：daily_digest 總預算 50K tokens（system_prompt 20%、skill_loading 15%）
- **設計方案**：
  - **預算監控**：`budget_guard.py` per-phase token budget，80% warning、100% 暫停 + ntfy 告警
  - **Context Compression**：`tools/context_compressor.py` BufferWindow + Summary 雙策略
  - **成本歸因**：results/*.json 新增 `token_usage` 欄位
- **可執行動作**：
  1. 在 `config/budget.yaml` 定義 per-phase budget（軟限制=容量×65%）
  2. 擴充 `tools/budget_guard.py` 加入 per-trace 成本歸因
  3. 實作 `tools/context_compressor.py`（BufferWindow + Summary 雙策略）
  4. 在 `post_tool_logger.py` 加入 session I/O 累計追蹤

---

## 四、Agent 編排進階

> **KB 研究指引**：採三階段路線：(1) 穩定性治理 → (2) 可觀測與治理統一 → (3) 架構收斂。避免把 harness 變成無法維護的大框架。

### 4.1 經理 Agent（Hierarchical 執行模式）

- **目標**：在 Team 模式引入「經理 Agent」，動態調整 Phase 2 並行策略
- **優先級**：P1（中期，高效益）
- **關聯**：CrewAI hierarchical 模式 45.9K Stars、`run-todoist-agent-team.ps1`
- **KB 補充**：
  - KB 研究指出「先選對複雜度層級」（Agentic AI 五大編排模式），避免過度使用多 Agent 架構
  - `autonomous_harness.py` 已有動態 policy 產生基礎可複用
  - 非破壞性動作應採 runtime override（帶 `reason`、`expires_at`），非硬重啟
- **反思**：OpenClaw（329K stars）明確拒絕 heavy orchestration 和 agent-hierarchy，強調「shipped fast」。是否需要經理 Agent 還是簡化現有 Phase 分界？
- **可執行動作**：
  1. 定義經理 Agent 的決策框架（任務複雜度評分 → 並行/順序決策）
  2. 先用 runtime override 機制試行，避免引入新的編排複雜度
  3. A/B 測試：比較固定並行 vs 動態編排的成功率與耗時

### 4.2 角色特化（Role + Goal + Backstory）

- **目標**：為各 Agent prompt 加入角色特化 frontmatter，強化輸出一致性
- **優先級**：P2（中期，低成本）
- **關聯**：CrewAI 模式、`prompts/team/todoist-*.md`
- **可執行動作**：
  1. 定義 role/goal/backstory 的 frontmatter schema
  2. 為 5 個高頻自動任務加入角色 frontmatter
  3. 觀察 1 週輸出品質變化

### 4.3 Human-in-the-Loop（干預點配置）

- **目標**：高風險操作（git push、KB 大量匯入）需人工批准後才執行
- **優先級**：P2（中期）
- **關聯**：LangGraph 模式、ntfy-notify Skill
- **可執行動作**：
  1. 建立 `config/approval-rules.yaml`：定義哪些 task_type 需要批准
  2. Phase 2 完成後寫入 `state/approval-pending-{task_key}.json` + 發送 ntfy 通知
  3. Phase 3 檢查批准狀態才執行

### 4.4 SLO/Error Budget 治理 ✅ → 持續監控

- **狀態**：ADR-20260320-032 **已完成**
- **成果**：`tools/slo_budget_manager.py`、`state/failure-stats.json`、`config/slo.yaml` 均已建立
- **殘餘追蹤**：daily_success_rate=0.865 仍低於 0.95 門檻，持續觀察 budget burn 與 failure-class 分布

---

## 五、技術棧擴充

### 5.1 MCP 原生整合（Todoist / KB / ntfy）

- **目標**：將核心 API 呼叫封裝為 MCP Server，取代手工 curl
- **優先級**：P1（短期～中期）
- **關聯**：KB 筆記 `f1667a4e`、`027c1ad9`
- **KB 研究發現**：
  - Todoist 官方 MCP（`todoist-ai`）適合互動查詢；排程仍建議 HTTP API
  - Chrome DevTools MCP 五層架構（協定層→CLI→控制層→能力層→輸出優化層）可作為設計參考
- **可執行動作**：
  1. 評估 Todoist MCP Server 用於互動場景的混合架構
  2. 用 `mcp-builder` Skill 建立 KB MCP Server 原型
  3. 設計 MCP ↔ 現有 Skill 的過渡方案

### 5.2 Hono — 超輕量 Edge-First Web 框架

- **目標**：評估 Hono 作為 `bot/groq-relay.js`（Express）替代方案
- **優先級**：P2（中期）
- **關聯**：KB 筆記 `7d524b1e`（Hono 深度研究）
- **可執行動作**：
  1. 建立 `bot/groq-relay-hono.js` 原型
  2. 效能對比測試：Express vs Hono
  3. 評估 Cloudflare Workers 部署方案

### 5.3 加入 LlamaIndex + Mistral 3.2 Small

- **目標**：引入 LlamaIndex 作為 RAG 框架 + Mistral 3.2 Small 作為輕量推理模型
- **優先級**：P2（中期）
- **關聯**：`config/llm-router.yaml`
- **可執行動作**：
  1. 在 `llm-router.yaml` 新增 `mistral_small` 路由規則
  2. 建立 `tools/llamaindex_rag.py` 原型
  3. 比較 LlamaIndex RAG vs 現有 hybrid search 的召回率

### 5.4 Langfuse — LLM 可觀測性平台

- **目標**：評估 Langfuse 作為 Agent 可觀測性補充方案
- **優先級**：P2（中期）
- **關聯**：KB 筆記 `c1b8729f`、`28f2e391`
- **KB 補充**：Langfuse 以 OpenTelemetry 為基底，per-trace 成本歸因已是生產標準；可映射為本專案的 execution-journal
- **可執行動作**：
  1. 評估 Langfuse 自架 vs 雲端版成本
  2. 設計 `post_tool_logger.py` → Langfuse 的資料橋接方案

### 5.5 統一 Execution Trace Schema ✅ → 持續擴充

- **狀態**：ADR-20260319-030 **已完成**（span_id 與 cause_chain 欄位已建立）
- **殘餘追蹤**：
  - OpenTelemetry GenAI 規格已覆蓋 agent spans、MCP semantic conventions，可持續對齊
  - 補上 agent-to-agent message 結構化（與 2.5 通訊機制搭配）

### 5.6 Machine-readable Skill Registry ✅

- **狀態**：ADR-20260320-033 **已完成**
- **成果**：`tools/sync_skill_registry.py`、`context/skill-registry.json`（52 Skills）已建立

---

## 六、對外整合與部署

### 6.1 Cloudflare Workers 部署探索

- **目標**：將部分 API 服務遷移到 Edge，降低本機依賴
- **優先級**：P3（長期）
- **關聯**：5.2 Hono 框架評估
- **可執行動作**：
  1. 盤點可遷移的服務：`groq-relay`、`pingtung-news MCP`
  2. 評估 D1（SQLite on Edge）取代本地快取 JSON 的可行性

### 6.2 LINE Webhook + Gun Relay 整合

- **目標**：完善 LINE 通知管道，與現有 Gun.js 聊天室整合
- **優先級**：P2（中期）
- **關聯**：`docs/plans/line-webhook-gun-relay-plan.md`、待納入 N2
- **可執行動作**：
  1. Review 現有計畫文件，確認可行性
  2. 實作 LINE → Gun.js 的雙向訊息橋接

### 6.3 Hook 宣告式 Registry ✅

- **狀態**：ADR-20260319-031 **已完成**
- **成果**：hook-registry 已上線，支援可排序、可停用、可分 stage 的規則管理

---

## 七、內容與研究

### 7.1 知識庫深化策略

- **目標**：提升 KB 筆記品質與覆蓋面，支撐 Agent 決策
- **優先級**：持續進行
- **關聯**：`kb-research-strategist` Skill（五階段模型）、`kb-curator` Skill
- **已完成基礎**：
  - N7 研究飽和度分析 → ADR-20260322-039 **已完成**（`tools/research_saturation_analyzer.py`、`analysis/research-topic-clusters.json` 已建立）
  - N8 Skill 異常偵測 → ADR-20260322-038 **已完成**（`tools/skill_anomaly_detector.py`、`context/skill-health-report.json` 已建立）
- **可執行動作**：
  1. 定期執行 `kb-curator` 去重與品質評分
  2. 運用 `research_saturation_analyzer.py` 自動降低已飽和主題的研究頻率（saturation_score > 0.7 → 暫停/轉子主題）
  3. 優先深化 saturation_score < 0.3 的高潛力主題

### 7.2 Prompt 與模板版本化 ✅

- **狀態**：ADR-20260320-034 **已完成**
- **成果**：`tools/add_prompt_versions.py`、`state/template-versions.json`（65 檔案）已建立
- **殘餘追蹤**：新產生的結果檔需包含 `prompt_version` 欄位

---

## 已完成歸檔

> 以下項目已完成實施，保留作為歷史參考。

| 項目 | ADR | 完成日期 | 備註 |
|------|-----|---------|------|
| 3.1 跨平台路徑清理 | ADR-025 | 2026-03-18 | $PSScriptRoot 相對路徑 |
| 3.2 輸入驗證強化 | ADR-026 | 2026-03-18 | validate_json_schema() |
| 3.3 Context Budget Guard | ADR-022 | 2026-03-17 | context-budget-monitor Skill |
| 3.4 快取 TTL 調優 | ADR-029 | 2026-03-19 | adaptive_ttl 配置 |
| 4.4 SLO/Error Budget | ADR-032 | 2026-03-20 | slo_budget_manager.py |
| 5.5 Trace Schema | ADR-030 | 2026-03-19 | span_id + cause_chain |
| 5.6 Skill Registry | ADR-033 | 2026-03-20 | skill-registry.json (52) |
| 6.3 Hook Registry | ADR-031 | 2026-03-19 | 宣告式 hook 規則 |
| 7.2 模板版本化 | ADR-034 | 2026-03-20 | template-versions.json (65) |
| 1.1 公平輪轉 | ADR-027 | 2026-03-18 | fairness-hint 預計算 |
| 1.2 Phase 2 快照 | ADR-028 | 2026-03-18 | snapshot-resume |
| N7 研究飽和度 | ADR-039 | 2026-03-22 | research_saturation_analyzer.py |
| N8 Skill 異常 | ADR-038 | 2026-03-22 | skill_anomaly_detector.py |

---

## 參考資源

- [Langfuse AI Agent 比較](https://langfuse.com/blog/2025-03-19-ai-agent-comparison)
- [OpenTelemetry Traces 概念](https://opentelemetry.io/docs/concepts/signals/traces/)
- [Azure Cache-Aside Pattern](https://learn.microsoft.com/en-us/azure/architecture/patterns/cache-aside)
- [Google SRE Error Budget Policy](https://sre.google/workbook/error-budget-policy/)
- [Google SRE Monitoring Distributed Systems](https://sre.google/sre-book/monitoring-distributed-systems/)
- KB 筆記：AI Agent 記憶系統研究（`b1640648`）
- KB 筆記：Hindsight 四網路記憶架構（`1adc3ffd`）
- KB 筆記：Mastra Observational Memory（`14c8dbd1`）
- KB 筆記：Agent Context Window 治理（`ec882cc4`）
- KB 筆記：Token 經濟學優化（`79d30f1a`）
- KB 筆記：可觀測性缺口與自愈升級（`854fb55b`）
- KB 筆記：Agent Harness 優化方案（`e07d958e`）
- KB 筆記：Harness 分議題研究（`fee90ebe`）
- KB 筆記：LINE Bot Agentic 管線（`418e0c9c`）
- 專案改進積壓：`context/improvement-backlog.json`
- 架構決策記錄：`context/arch-decision.json`
